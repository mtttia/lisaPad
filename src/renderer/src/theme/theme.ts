import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#ff0000'
    },
    secondary: {
      main: '#00ff00'
    }
  }
})

export default theme
